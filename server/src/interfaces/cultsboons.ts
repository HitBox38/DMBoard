export interface cultsboons {
  name: string;
  source: string;
  page: number;
  type: string;
  entries: string[];
  otherSources?: object[];
  goal?: object;
  cultists?: object;
  signaturespells?: object;
  reprintedAs?: object[];
}
